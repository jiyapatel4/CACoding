package use_case.clear_users;

// TODO Complete me

import use_case.login.LoginInputData;

public interface ClearInputBoundary {
    // no input data from user within the view, so execute() requires no parameter
    void execute();
}
